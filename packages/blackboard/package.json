{
  "name": "@chardesk/blackboard",
  "version": "0.0.0",
  "description": "Local read-only Blackboard for a directly edited CharDesk file",
  "private": true,
  "type": "module",
  "bin": {
    "chardesk-blackboard": "./dist/cli.js"
  },
  "scripts": {
    "prebuild": "npm run build -w @chardesk/document && npm run build -w @chardesk/protocol",
    "build": "tsc -p tsconfig.json",
    "pretypecheck": "npm run build -w @chardesk/document && npm run build -w @chardesk/protocol",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "npm run build",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@chardesk/document": "*",
    "@chardesk/protocol": "*"
  },
  "devDependencies": {
    "@types/node": "^24.10.4",
    "typescript": "~5.9.3",
    "vitest": "^4.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
