当前的多模态场景下,一张截图对模型意味着 1-2000 token和数十个视觉patch,和大量噪声颜色和像素,使用非常方便,但在工业场景下的负担也很明显：

注意力漂移，影响长程任务

> 如果代码报错
时截图几百行报错日志甩给模型，我们在边上直接血压飙升。模型推理时全是颜色、样式等噪声

单向不可逆，模型在修改和回传图片时需要整体重绘，无法在多轮任务中持续、精准交互

> 不是说多模态不行，而是当下多模态底座还是 llm，而 llm 可能还有很大的潜力没有被挖掘

---
人类喜欢并行处理信息，互联网发展诞生了 GUI、页面、图标...我们可以瞬间抓取核心表征

llm 底座只要还是 transformer，就会偏好于符号序列的串行处理

过去我们在让两个物种强行适应：让 llm 想办法处理人类的 GUI；让人类读纯文本流。有没有什么介质可以同时满足我们和 llm 的偏好？
对人类而言可以直观承载视觉信息；而对 llm 而言是原生 token 序列且低噪声

早期 gpt-4 会偶尔输出 emoji，色彩和生动的表达切合了人类的偏好，不再是冰冷的文本流，于是这样的训练数据在标注时得分更高，导致 4o 发布的时候非常爱滥用 emoji

> 让我们开始吧！🚀
> 这里有 3 个关键点：🎯
> 这太神奇了！😀

而 😀 在潜空间内是纯粹文本 token；似乎揭露了某些文本天然具有承载视觉信息的能力，而我们过去 llm 的表达只是冰山一角

emoji 和中文都来自于 unicode 的极小选区，unicode 是人类在计算机记录文明而打造的字符集标准，从甲骨文、数字符号、电路图到中日韩文，以及丰富的私有选区，如 nerd font：

一套纯文本的图形金矿，可这么久了为什么只有 emoji 跑出来了？

unicode 极度依赖字体资源，移动时代苹果和安卓会预装 emoji 字体。这让我们以为 emoji 是计算机世界的底层标准，结论也很明显了，剩下的 unicode 并不是模型不会写，而是字体不支持

思路也变得清晰，如果我们可以统一工程协定，比如统一网格宽度和拓展字体包，我们可以解锁出模型更丰富的能力

看过我以前文章的佬友也会意识到我过去的文章表达里会融入这一特性：

```

```

我们可以更进一步利用预训练的数据，加入 ansi 序列协议（可以理解为让文本具有颜色属性）

> 现在的分词器还不能分词 `\x1b[31m` 或 `\033[31m` 前缀
> 我一般为了节省 token 会让模型省略前缀，只表达 `[31m`

于是我们可以实现：

```

```

---

同时实现了拓展模型表达、交互形态从一维变为二维，下面是几个人机协作技巧：

### 前端设计
很多时候我们做前端是直接甩给模型一张图或者一个链接；再进一步可能在 figma 做好设计再导出结构化内容让模型实现。但是在微调时却难以和模型对齐想法

### 可视化讲解
#### 流程图
很多时候我们会用 mermaid 来理解一个流程的细节，可是这个过程是单向的：llm 输出 mermaid 语法很容易，但是当 user 在编辑 mermaid 时，我们很少会去花很大精力去改动一个流程，于是我们和 llm 消费同一套表达形式：

####  讲解 xx
xx

#### 拓展 emoji
在过往的 gpt 模型会很喜欢用 emoji 来迎合使用者；但是慢慢的人们发现这种形式会更加厌恶，导致 emoji 成了一个很讨厌的表达形式
emoji 常规只有自己的颜色，可是当我们拓展 font 后，我们可以给 emoji 拓展颜色的维度，emoji 的表达形式便足够丰富

当我们只能用 unicode 表达内容的时候，我们有数万个原子，而当我们给 canvas 拓展了加粗，斜体，下划线｜颜色｜高光以后，我们的可表达的丰富度已经足够呈现我们想呈现的所有内容，比如我们可以表达 Pokemon：
```

```

表达 xxx ：
```

```

甚至我们可以呈现一个邪修的操作系统——真正的 LLMOS：


以及我们可以制作受控的科研图:
